# 全日健康 · 无服务器部署指南

> 纯前端应用,数据 100% 存浏览器本地(localStorage),**不需要任何云服务器**。
> 本文档覆盖:本地局域网 → 内网穿透 → 公网访问(串流) → 本地 AI API 导入。

---

## 一、最快上手:本机打开

| 方式 | 命令 | 说明 |
|------|------|------|
| 直接打开 | 双击 `index.html` | 零依赖,但**不能**用局域网/公网访问,浏览器直连 AI API 可能被 CORS 拦截 |
| Node.js 启动 | `node server.js` | ✅ 推荐。自带 API 代理,手机可访问 |
| 一键脚本 | `./start.sh` (Mac/Linux) / `start.bat` (Windows) | 自动选择 node 或 python |
| Python 启动 | `python3 -m http.server 3111` | 只有静态服务,无 API 代理 |

启动后本机访问:`http://localhost:3111`

---

## 二、手机访问(同一局域网)

1. 启动服务:`node server.js`(或 `./start.sh`)
2. 终端会打印**局域网地址**,例如:
   ```
   手机/平板(同一 Wi-Fi):
              http://192.168.1.105:3111
   ```
3. 手机(或平板)连**同一个 Wi-Fi**,浏览器打开 `http://192.168.1.105:3111`

> ⚠️ 如果打不开:检查电脑防火墙是否放行该端口(Windows 防火墙 / macOS 防火墙)。
> 手机 iOS Safari 首次打开后,点「分享 → 添加到主屏幕」即可像 App 一样全屏使用(PWA)。

---

## 三、公网访问(内网穿透)

没有公网 IP/云服务器也能让外面访问。任选一种:

### 方案 A:cpolar(国内速度快,推荐)
```bash
# 1. 安装(需注册免费账号:https://www.cpolar.com)
curl -L https://www.cpolar.com/static/downloads/install-release-cpolar.sh | sudo bash

# 2. 启动服务(另一终端)
node server.js

# 3. 穿透
cpolar http 3111
```
完成后会得到 `https://xxxx.cpolar.top` 公网地址,手机 4G/5G 也能访问。

### 方案 B:ngrok(国外,免费额度够用)
```bash
# 安装:https://ngrok.com/download
ngrok http 3111
# 得到 https://xxxx.ngrok-free.app
```

### 方案 C:Cloudflare Tunnel(免安装客户端?需 cloudflared)
```bash
# 1. 下载 cloudflared:https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/
# 2. 直接创建临时隧道(无需登录):
cloudflared tunnel --url http://localhost:3111
# 得到 https://xxxx.trycloudflare.com
```
✅ 优点:免费、无需注册、自带 HTTPS;**缺点**:临时域名,重启后地址变化。

### 方案 D:frp(自建服务器,最稳)
```bash
# 需要一台有公网 IP 的服务器(轻量云/旧电脑),配置文件 frps.ini(服务端) / frpc.ini(客户端)
# 服务端 frps.ini:
[common]
bind_port = 7000

# 客户端 frpc.ini:
[common]
server_addr = 你的服务器IP
server_port = 7000

[web]
type = http
local_ip = 127.0.0.1
local_port = 3111
custom_domains = your.domain.com
```
```bash
# 服务端运行
frps -c frps.ini
# 客户端运行
frpc -c frpc.ini
```

### 方案 E:Tailscale(异地组网"串流",仅自己设备可访问,最安全)
```bash
# 1. 电脑和手机都装 Tailscale(https://tailscale.com),登录同一账号
# 2. 电脑上 node server.js 启动
# 3. 手机浏览器访问 http://<电脑的TailscaleIP>:3111
# 得到 100.x.x.x 内网地址,任何网络环境都能连,且只有你的设备能访问
```
✅ 优点:加密、私密、无公网暴露;**缺点**:访问者也需要装 Tailscale。

### 公网访问后的健康检查
```bash
curl https://你的公网地址/api/health
# 返回 {"ok":true,...} 即服务正常
```

---

## 四、公网串流(播放本地媒体)

「全日健康」本身是数据应用,若你希望把**电脑上的视频/音乐**也串流到手机,配合使用:

| 工具 | 用途 |
|------|------|
| VLC / Infuse(手机端) | 通过 WebDAV/SMB 串流电脑媒体库 |
| Jellyfin(自建媒体服务器) | 配合上面的内网穿透,手机浏览器直接看片 |
| 阿里云盘/夸克(有会员) | 公网串流的最省心替代 |

---

## 五、本地 AI API 导入(OpenAI 兼容)

### 云端 API(DeepSeek / 通义 / 智谱 / Kimi / 硅基流动等 14 家已内置预设)
1. 「更多」→ API 端点 → **快速选择**点一家(如 DeepSeek)
2. 填入你的 **API Key**(密钥只存浏览器本地,不上传)
3. 点 **「测试连接」** 验证
4. 开启 **「本地代理」**(通过 server.js 转发,避免浏览器直连 CORS 拦截)
5. 回到首页重新生成计划 → 使用 AI

### 本地 Ollama(完全离线、免费、隐私最强)
```bash
# 1. 电脑安装 Ollama:https://ollama.com
# 2. 拉模型(如 qwen2.5 / llama3)
ollama pull qwen2.5
# 3. 启动(默认端口 11434,并监听局域网)
OLLAMA_HOST=0.0.0.0:11434 ollama serve
#   或 Windows:设置环境变量 OLLAMA_HOST=0.0.0.0:11434 后启动 Ollama
```
设置页配置:
- 端点 URL:`http://<电脑局域网IP>:11434/v1/chat/completions`(例如 `http://192.168.1.105:11434/v1/chat/completions`)
- 模型:`qwen2.5`(或你拉取的模型名)
- API Key:随便填(如 `ollama`),**必须开启「本地代理」**——浏览器直连 http 端点会被拦截

> 公网访问时,本地代理经内网穿透把请求转发给本机 Ollama,同样可用。

### 从别处"导入"API 配置
目前 API Key/端点存在浏览器 localStorage。换设备/换浏览器时:
1. 原设备:「更多」→ 数据 → **导出 JSON**
2. 新设备:打开同一地址 → 「更多」→ 数据 → **导入 JSON**

---

## 六、数据安全与备份

| 特性 | 说明 |
|------|------|
| 存储位置 | 100% 浏览器 localStorage,**无云端服务器** |
| 加密 | 可选 AES-256-GCM(Web Crypto + PBKDF2),设置密码后所有数据加密存储 |
| 备份 | 设置页 → 数据 → 导出 JSON(建议每周导出一次) |
| 清理 | 换浏览器/清除站点数据 = 清空数据,记得先导出 |

---

## 七、常见问题

| 问题 | 解决 |
|------|------|
| 手机打不开局域网地址 | 检查:同一 Wi-Fi?防火墙放行?地址是否是终端打印的 192.168 地址? |
| 公网地址显示但 502 | 内网穿透进程要保持运行,先 `curl localhost:3111/api/health` 确认服务活着 |
| AI 不生效 | 设置页「测试连接」看报错;用代理模式;Ollama 确认已监听 0.0.0.0 |
| 想离线使用 | 首次打开后 Service Worker 已缓存全部资源,断网也能用;建议「添加到主屏幕」 |
| 数据丢了 | 从之前导出的 JSON 导入恢复 |

---

*本文档随项目一起分发,修改自 MIT 协议的「全日健康」项目,按需调整部署方式即可。*
